# Next.js example

```bash
# from repo root
npm run build
cd examples/nextjs-app
npm install
npm run dev
```

Uses a `"use client"` provider boundary so the App Router layout stays SSR-compatible while access state lives on the client.
