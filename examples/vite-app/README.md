# Vite example

```bash
# from repo root
npm run build
cd examples/vite-app
npm install
npm run dev
```

Switch between Admin / Manager / User to see protected sidebar links, buttons, and fallback UI.
