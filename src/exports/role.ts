import { Role as RoleComponent } from "../components/Role";

/** Role name string used throughout the API. */
export type Role = string;

/** Declarative role gate component. */
export const Role = RoleComponent;
