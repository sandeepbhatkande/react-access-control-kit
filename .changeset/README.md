# Changesets

This repository uses [Changesets](https://github.com/changesets/changesets) to manage versions and changelogs.

1. Run `npm run changeset` after a meaningful change.
2. Merge the version PR created by the Release workflow.
3. The workflow publishes to npm and creates a GitHub Release.
